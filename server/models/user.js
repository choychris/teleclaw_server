'use strict';

module.exports = function(User) {
  // User.exist = (user, cb) => {

  // };

  // User.remoteMethod(
  //   'exist',
  //   {
  //     accepts: {arg: user, type: 'object', http: {srouce: 'body'}},
  //     returns: {arg: }
  //   }
  // );
};
