image: node:6.11.1

services:
  - mongo:3.4

stages:
  - build
  - test
    
build:
  stage: build
  script:
    - npm install

test:
  stage: test
  dependencies:
    - build          
  script: 
    - npm run dev
